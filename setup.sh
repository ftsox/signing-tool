sudo apt update
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
nvm install --lts=hydrogen
npm install -g yarn
sudo apt install jq
which nvm
which yarn
ls -la
git clone https://github.com/ftsox/signing-tool.git
ls -la
cd signing-tool/
yarn
yarn build
nano .env
yarn build
which yarn
which node
which navm
nvm -version
nvm -v
which yarn
which node
ls -la
rm -rf node_modules
nvm install 18.20.2
nvm use v18.20.2
which yarn
npm install -g yarn
which yarn
yarn
yarn build
yarn tsc -b
ls -la
cd signing-tool/
which yarn
nvm use 18.20.2
which yarn
yarn build
bin/signing-tool auto
cd signing-tool/
screen
screen -r
ls -la
cd bin
ls -la
cd ..
git status
git config --get remote.origin.url
git diff
git pull
git status
ls -la
history
